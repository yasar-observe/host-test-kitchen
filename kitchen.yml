driver:
  name: ec2
  # aws_ssh_key_id: YOUR_AWS_SSH_KEY_ID
  region: us-west-2
  # availability_zone: YOUR_AWS_AVAILABILITY_ZONE
  instance_type: t2.micro
  # subnet_id: 	subnet-03560583eab482027
  # security_group_ids: YOUR_AWS_SECURITY_GROUP_IDS


provisioner:
  name: shell
  arguments: ['--observe_host', 'redacted', '--observe_token', 'redacted']    
  # inline: |
  #   #!/bin/bash
  #   sudo apt-get update

platforms:
  # - name: ubuntu-22.04
  - name: amazon-2
    driver:
      image_id: ami-04288abc8d2000768
  # - name: amazon-linux-2023
  # - name: rhel-9
  # - name: debian-12
    # driver:
    #   image_id: ami-xxxxxxxxxxxxxxxxx
    transport:
      upload_path: /tmp

verifier:
  name: inspec

suites:
  - name: default
    verifier:
      inspec_tests:
        - test/integration/default
    attributes: